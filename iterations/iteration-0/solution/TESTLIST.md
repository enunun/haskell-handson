# テストリスト(Iteration 0・解答例)

## 単体テスト

- [x] `total`：空のリストなら0を返す
- [x] `total`：要素が1つなら，その値を返す
- [x] `total`：複数の金額を足し合わせる
- [x] `formatYen`：金額の後ろに「円」を付ける
- [x] `formatYen`：0円を表示する

## 結合テスト

- [x] `run`：引数の金額の合計を`合計: 〜円`の1行で表示する
- [x] `run`：引数がなければ合計は0円
