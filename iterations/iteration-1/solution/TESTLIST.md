# テストリスト(Iteration 1・解答例)

## 単体テスト

- [x] `formatYen`：4桁の金額は，上から1桁目の後ろにカンマを入れる(`1,200円`)．既存の「金額の後ろに「円」を付ける」の例を3桁の金額に変える
- [x] `formatYen`：3桁の金額にはカンマを入れない(`999円`)
- [x] `formatYen`：6桁の金額は，上から3桁目の後ろにカンマを入れる(`123,456円`)
- [x] `formatYen`：7桁の金額は，3桁ごとにカンマを2つ入れる(`1,234,567円`)
- [x] `total`：`sum`を使わない再帰に書き直しても，既存のテストがすべて通る

## 結合テスト

- [x] `run`：合計を3桁区切りで表示する(既存のテストの期待値を`合計: 2,350円`に変える)
- [x] `run`：引数がなければ`支出はありません`と表示する(既存のテストの期待値を変える)
