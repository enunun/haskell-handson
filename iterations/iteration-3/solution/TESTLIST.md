# テストリスト(Iteration 3・解答例)

## 単体テスト

- [x] `summarize`：支出がなければ空のリストを返す
- [x] `summarize`：支出が1件なら，その費目と金額だけを返す
- [x] `summarize`：同じ費目の支出を足し合わせる
- [x] `summarize`：費目の定義順に並べる
- [x] `total`：`foldr`で書き直しても，既存のテストがすべて通る

## 結合テスト

- [x] `run`：同じ費目の支出を，費目別の小計でまとめて表示する
- [x] `run`：既存の2つのテスト(`食費:1200 交通費:350`と`1200 350 800`)の期待値に，費目別の小計の行を足す
